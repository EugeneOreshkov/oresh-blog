{
   "version": "0.2.0",
   "configurations": [
       {
           "name": "Python: Tests",
           "type": "pytest",
           "request": "launch",
           "program": "${workspaceFolder}/run.py",
           "console": "integratedTerminal",
           "env": {
           "PYTHONPATH": "${workspaceFolder}"
           }
       }
   ]
}